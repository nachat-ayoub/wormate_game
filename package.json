{
  "name": "2d-test-game",
  "version": "1.0.0",
  "source": "src/index.html",
  "license": "MIT",
  "scripts": {
    "dev": "rm -rf ./dist && rm -rf ./.parcel-cache/ && parcel --port 3000",
    "build": "parcel build"
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  },
  "assets": [
    "assets/**/*"
  ]
}
